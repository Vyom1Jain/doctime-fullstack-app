# Development profile - override specific settings for local dev
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/doctime?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: password
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

logging:
  level:
    com.doctime: DEBUG
    org.springframework.web: DEBUG
